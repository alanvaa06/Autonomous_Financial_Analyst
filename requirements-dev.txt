# Development / test dependencies. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Not installed on the public HF Space — runtime image stays slimmer.

-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
responses>=0.25
